package ru.netology.sqr;

public class SQRService {
    public int MathCalculate(int limit1, int limit2) {
        int count = 0;
        for (int i = 10; i <= 50; i++) {
            if (i * i >= limit1)

            if (i * i <= limit2) {
                count++;}
            }
            return count;
        }
    }
